# 偶遇自定义

**ID:** e6664859-3e99-448f-b4f1-0bb7b28b832d

**启用状态:** true

**Markdown Only:** true

**Prompt Only:** false

**Run On Edit:** true

**Placement:** [1,2,3]

## 查找正则

```regex
【encounter_creation_panel】
```

## 替换字符串

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>阴阳合欢 · 猎艳偶遇</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: "Microsoft YaHei", sans-serif; background: transparent; padding: 8px; color: #e8d5b5; }

        /* 面板主结构：古朴暗红情欲风 */
        .creation-container {
            max-width: 680px; margin: 0 auto;
            background: linear-gradient(145deg, #1c0a0c 0%, #0d0405 100%);
            border: 1px solid #5a1e26; border-radius: 8px;
            padding: 24px; box-shadow: 0 8px 32px rgba(90, 30, 38, 0.4);
            position: relative; overflow: hidden;
        }

        .creation-container::before {
            content: ''; position: absolute; top: 0; left: 0; width: 100%; height: 4px;
            background: linear-gradient(90deg, transparent, #b22222, #d4af37, #b22222, transparent);
        }

        .panel-title {
            text-align: center; font-size: 22px; font-weight: bold; color: #d4af37;
            margin-bottom: 20px; letter-spacing: 4px; text-shadow: 0 2px 4px rgba(0,0,0,0.8);
            border-bottom: 1px solid rgba(212, 175, 55, 0.3); padding-bottom: 12px;
        }

        .section-title {
            font-size: 16px; color: #dca5a5; margin: 24px 0 12px;
            display: flex; align-items: center; gap: 8px; letter-spacing: 1px;
        }
        .section-title::before { content: '✧'; color: #b22222; font-size: 14px; }
        .section-title::after { content: ''; flex-grow: 1; height: 1px; background: linear-gradient(90deg, #5a1e26, transparent); }

        .form-row { display: flex; margin-bottom: 10px; align-items: center; gap: 12px; flex-wrap: wrap; }
        .form-label { flex: 0 0 85px; font-size: 14px; color: #b59b9b; text-align: right; }
        .form-input-group { flex: 1; display: flex; gap: 6px; align-items: center; flex-wrap: wrap; }

        input[type="text"], select, textarea {
            background: #160809; border: 1px solid #4a191f; color: #e8d5b5;
            padding: 8px 10px; border-radius: 4px; font-size: 14px; outline: none;
            transition: border-color 0.3s, box-shadow 0.3s;
        }
        input[type="text"]:focus, select:focus, textarea:focus { border-color: #d4af37; box-shadow: 0 0 5px rgba(212, 175, 55, 0.3); }
        input::placeholder, textarea::placeholder { color: #5c4346; }

        select { flex: 0 0 auto; min-width: 110px; }
        input[type="text"] { flex: 1; min-width: 120px; }

        .form-row-full { display: flex; flex-direction: column; gap: 4px; margin-bottom: 14px; }
        .form-row-full .form-label { text-align: left; font-size: 13px; color: #cfa8a8; }
        textarea { width: 100%; resize: vertical; min-height: 55px; }

        .btn-group { display: flex; gap: 12px; margin-top: 25px; flex-wrap: wrap; }
        button { padding: 12px; font-size: 15px; font-weight: bold; text-align: center; border: none; border-radius: 6px; cursor: pointer; letter-spacing: 2px; transition: all 0.2s; flex: 1; min-width: 200px; }
        .btn-random { background: #2a0f12; color: #dca5a5; border: 1px solid #5a1e26; }
        .btn-random:hover { background: #3a1519; border-color: #d4af37; }
        .submit-btn { background: linear-gradient(135deg, #7b1e28 0%, #4a0d14 100%); color: #d4af37; border: 1px solid #8b2331; box-shadow: 0 4px 10px rgba(0,0,0,0.5); }
        .submit-btn:hover { transform: translateY(-2px); box-shadow: 0 6px 15px rgba(139, 35, 49, 0.6); text-shadow: 0 0 5px #d4af37; }

        .hint { font-size: 12px; color: #8a6468; margin-left: 6px; font-weight: normal; }
        #world_hint { text-align: center; font-size: 12px; color: #8892b0; margin-bottom: 10px; font-style: italic; }

        @media (max-width: 500px) {
            .form-row { flex-direction: column; align-items: flex-start; gap: 4px; }
            .form-label { text-align: left; flex: none; width: 100%; font-size: 13px; }
            .form-input-group { width: 100%; flex-direction: row; }
            select { flex: 1; }
        }
    </style>
</head>
<body>
    <div class="creation-container" id="content">
        <div class="panel-title">红尘猎艳 · 狩猎男鼎</div>
        <div id="world_hint">正在读取世界法则...</div>

        <!-- 偶遇者设定 (自动从变量读取女性分身) -->
        <div class="section-title">出猎娇躯</div>
        <div class="form-row">
            <div class="form-label" style="color:#d4af37;">狩猎者</div>
            <div class="form-input-group">
                <select id="sel_encounter_body" style="width:100%; border-color:#d4af37;">
                    <option value="本体">本体 (读取中...)</option>
                </select>
            </div>
        </div>

        <!-- 猎物基础 -->
        <div class="section-title" style="color:#d4af37;">猎物皮相</div>

        <div class="form-row">
            <div class="form-label">男方姓名</div>
            <div class="form-input-group"><input type="text" id="p_name" placeholder="如 龙傲天"></div>
        </div>

        <div class="form-row">
            <div class="form-label">骨龄岁数</div>
            <div class="form-input-group">
                <select id="sel_p_age" onchange="updateInput('p_age', this.value)">
                    <option value="">- 择岁 -</option>
                    <option value="18岁">18岁</option><option value="21岁">21岁</option>
                    <option value="25岁">25岁</option><option value="28岁">28岁</option>
                    <option value="35岁">35岁</option><option value="45岁">45岁</option>
                </select>
                <input type="text" id="p_age" placeholder="选预设或自填">
            </div>
        </div>

        <div class="form-row">
            <div class="form-label">尘世身份</div>
            <div class="form-input-group">
                <select id="sel_p_job" onchange="updateInput('p_job', this.value)">
                    <option value="">- 身份 -</option>
                    <!-- JS注入男职业 -->
                </select>
                <input type="text" id="p_job" placeholder="选预设或自填">
            </div>
        </div>

        <div class="form-row">
            <div class="form-label">皮相&体格</div>
            <div class="form-input-group" style="gap:4px;">
                <select id="sel_p_face" onchange="updateInput('p_face', this.value)" style="flex:1;">
                    <option value="">-相貌-</option><option value="丑陋油腻">丑陋油腻</option>
                    <option value="平庸粗犷">平庸粗犷</option><option value="英俊刚毅">英俊刚毅</option>
                    <option value="清秀白净">清秀白净</option><option value="俊朗风流">俊朗风流</option>
                    <option value="邪魅狂狷">邪魅狂狷</option>
                </select>
                <input type="text" id="p_face" placeholder="相貌" style="flex:1;">

                <select id="sel_p_body" onchange="updateInput('p_body', this.value)" style="flex:1;">
                    <option value="">-体格-</option><option value="瘦弱单薄">瘦弱单薄</option>
                    <option value="大腹便便">大腹便便</option><option value="匀称修长">匀称修长</option>
                    <option value="精壮有力">精壮有力</option><option value="肌肉虬结">肌肉虬结</option>
                </select>
                <input type="text" id="p_body" placeholder="体格" style="flex:1;">
            </div>
        </div>

        <div class="form-row">
            <div class="form-label">性情本心</div>
            <div class="form-input-group">
                <select id="sel_p_personality" onchange="updateInput('p_personality', this.value)">
                    <option value="">- 性格 -</option>
                    <!-- JS注入男性格 -->
                </select>
                <input type="text" id="p_personality" placeholder="选预设或自填">
            </div>
        </div>

        <div class="form-row">
            <div class="form-label">隐秘性癖</div>
            <div class="form-input-group">
                <select id="sel_p_fetish" onchange="updateInput('p_fetish', this.value)">
                    <option value="">- 性癖 -</option>
                    <!-- JS注入性癖 -->
                </select>
                <input type="text" id="p_fetish" placeholder="选预设或自填">
            </div>
        </div>

        <!-- 男鼎肉体阳脉细节 -->
        <div class="section-title" style="color:#d4af37;">阳脉秘征</div>

        <div class="form-row">
            <div class="form-label">阳具尺寸</div>
            <div class="form-input-group">
                <input type="text" id="p_dick_size" placeholder="如：长18cm 粗5cm">
            </div>
        </div>

        <div class="form-row">
            <div class="form-label">肉茎形态</div>
            <div class="form-input-group">
                <select id="sel_p_dick_shape" onchange="updateInput('p_dick_shape', this.value)">
                    <option value="">- 择形 -</option>
                    <option value="短小细软">短小细软</option><option value="笔挺直长">笔挺直长</option>
                    <option value="粗长黑紫">粗长黑紫</option><option value="紫筋怒突">紫筋怒突</option>
                    <option value="龟头巨硕">龟头巨硕</option><option value="诡异倒钩">诡异倒钩</option>
                </select>
                <input type="text" id="p_dick_shape" placeholder="阳具形态">
            </div>
        </div>

        <div class="form-row">
            <div class="form-label">精关质量</div>
            <div class="form-input-group" style="gap:4px;">
                <select id="sel_p_sperm_q" onchange="updateInput('p_sperm_q', this.value)" style="flex:1;">
                    <option value="">- 精质 -</option><option value="清稀寡淡">清稀寡淡</option>
                    <option value="浓稠白浊">浓稠白浊</option><option value="滚烫阳火">滚烫阳火</option>
                    <option value="极品纯阳元精">极品纯阳元精</option>
                </select>
                <input type="text" id="p_sperm_q" placeholder="质量" style="flex:1;">

                <select id="sel_p_sperm_v" onchange="updateInput('p_sperm_v', this.value)" style="flex:1;">
                    <option value="">- 精量 -</option><option value="几滴即竭">几滴即竭</option>
                    <option value="量大如泉">量大如泉</option><option value="连绵不绝">连绵不绝</option>
                    <option value="恐怖海量">恐怖海量</option>
                </select>
                <input type="text" id="p_sperm_v" placeholder="射精量" style="flex:1;">
            </div>
        </div>

        <div class="form-row">
            <div class="form-label">体质耐受</div>
            <div class="form-input-group">
                <select id="sel_p_stamina" onchange="updateInput('p_stamina', this.value)">
                    <option value="">- 耐受度 -</option>
                    <option value="极易暴毙">极易被榨干暴毙</option><option value="常人耐力">常人耐力</option>
                    <option value="气血旺盛">气血旺盛极耐采补</option><option value="龙精虎猛">龙精虎猛</option>
                </select>
                <input type="text" id="p_stamina" placeholder="耐采补程度">
            </div>
        </div>

        <!-- 推进暗线与天缘巧合 -->
        <div class="section-title">天缘巧合</div>

        <div class="form-row">
            <div class="form-label">偶遇地界</div>
            <div class="form-input-group">
                <select id="sel_scene_place" onchange="updateInput('scene_place', this.value)">
                    <option value="">- 择地 -</option>
                    <option value="幽暗无人的小巷">幽暗小巷</option><option value="高档私密酒吧">高档酒吧</option>
                    <option value="林荫道/小树林">野外/树林</option><option value="深夜空旷电梯">空旷电梯</option>
                    <option value="酒店幽长走廊">酒店走廊</option><option value="男方的私人宅邸">私人宅邸</option>
                    <option value="硝烟弥漫的废墟">废墟角落</option>
                </select>
                <input type="text" id="scene_place" placeholder="选预设或自填">
            </div>
        </div>

        <div class="form-row">
            <div class="form-label">彼时状态</div>
            <div class="form-input-group">
                <select id="sel_scene_status" onchange="updateInput('scene_status', this.value)">
                    <option value="">- 状态 -</option>
                    <option value="高高在上发号施令">高高在上</option><option value="正仗势欺人寻欢作乐">仗势欺人</option>
                    <option value="被人暗算身受重伤">身受重伤</option><option value="误中春药浑身燥热">误中春药</option>
                    <option value="正狂妄地凝视着你">狂妄注视</option><option value="被你刻意制造的意外撞到">刻意碰撞</option>
                </select>
                <input type="text" id="scene_status" placeholder="相遇时他在做什么">
            </div>
        </div>

        <div class="section-title" style="margin-top:16px;">推进暗线</div>
        <div class="form-row-full">
            <div class="form-label">猎物隐情 <span class="hint">(额外设定，如他其实是个处男，或有隐秘伤病)</span></div>
            <textarea id="extra_setting" placeholder="如有其他需要补充的设定请填入..."></textarea>
        </div>

        <div class="form-row-full">
            <div class="form-label">妖姬手段 <span class="hint">(决定你遇见他之后的第一个魅惑/压制动作)</span></div>
            <textarea id="start_setting" placeholder="例如：假装柔弱跌入他怀里顺势摸向胯下，或直接施展迷魂印将其控制..."></textarea>
        </div>

        <div class="btn-group">
            <button class="btn-random" onclick="randomizeAll()">🎲 随机猎物</button>
            <button class="submit-btn" onclick="submitCreation()">锁定男鼎 · 诱捕开始</button>
        </div>
    </div>

    <script>
        // 字典：根据世界观匹配的男性身份词库
        const DICT_JOBS = {
            "现代都市": { p: ["富二代纨绔", "健身教练", "部门男上司", "包养金主", "纯情男大学生", "高利贷债主", "当红小鲜肉"] },
            "古代盛世": { p: ["朝廷权臣", "风流才子", "粗鄙武将", "纨绔衙内", "皇族王爷", "护院武僧", "强抢民女的恶霸"] },
            "古代乱世": { p: ["割据军阀", "嗜血悍卒", "游牧蛮族首领", "落草山大王", "坞堡少主", "冷酷死士"] },
            "末世废土": { p: ["掠夺者头目", "避难所长官", "精锐雇佣兵", "变异肌肉巨汉", "黑商"] },
            "赛博朋克": { p: ["公司高管", "狂暴赛博精神病", "漩涡帮老大", "顶尖义体佣兵", "黑客财阀公子"] },
            "西幻世界": { p: ["圣殿骑士长", "巨龙化身", "帝国大公", "魔法导师", "捕奴队长", "深渊魔王"] },
            "蒸汽朋克": { p: ["飞艇船长", "蒸汽财阀老爷", "齿轮帮老大", "疯癫发明家", "机械监工"] },
            "异世界": { p: ["天降勇者", "S级冒险者", "贵族领主", "帝国皇子", "奴隶商人"] },
            "都市爽文": { p: ["气运龙王", "隐世神医", "富二代反派", "贴身男保镖", "古武老祖"] },
            "中式诡异": { p: ["乡绅地主", "赶尸道士", "军阀军官", "怨气老鬼", "恶霸少爷"] },
            "日式都市": { p: ["极道少主", "虚伪政客", "嚣张男校霸", "金主欧吉桑", "催收员"] },
            "修真界": { p: ["宗门老祖", "狂傲真传大弟子", "魔道枭雄", "道貌岸然掌教", "剑宗天骄"] }
        };
        const DICT_M_PERSONALITY = ["狂傲霸道,唯我独尊", "阴暗病态,占有欲强", "道貌岸然,伪善冷酷", "粗鄙暴躁,只懂发泄", "冷酷禁欲,高高在上", "卑微舔狗,盲目痴情", "风流倜傥,处处留情"];
        const DICT_M_FETISH = ["无特殊癖好", "极端绿帽癖(喜欢看妻子被别人上)", "足交控(极度迷恋丝袜与双足)", "受虐狂(喜欢被辱骂践踏)", "母乳控(对乳房有病态执念)", "野外露出癖", "处女情结(极其病态)"];

        let currentActiveWorld = "现代都市";

        async function loadWorldView() {
            try {
                let retries = 0;
                while (typeof window.parent.Mvu === 'undefined' && retries < 20) {
                    await new Promise(r => setTimeout(r, 200));
                    retries++;
                }

                let msgId = null;
                if (window.parent.getCurrentMessageId) msgId = window.parent.getCurrentMessageId();
                if (msgId === null || msgId === undefined) {
                     if (window.parent.SillyTavern && window.parent.SillyTavern.getContext) {
                         const ctx = window.parent.SillyTavern.getContext();
                         if (ctx && ctx.chat && ctx.chat.length > 0) msgId = ctx.chat.length - 1;
                     }
                }

                if (window.parent.Mvu && msgId !== null) {
                    const rawVars = window.parent.Mvu.getMvuData({ type: 'message', message_id: msgId });
                    if(rawVars && rawVars.stat_data && rawVars.stat_data["世界与本源"] && rawVars.stat_data["世界与本源"]["当前世界观"]) {
                        const savedWorld = rawVars.stat_data["世界与本源"]["当前世界观"];
                        if (DICT_JOBS[savedWorld]) {
                            currentActiveWorld = savedWorld;
                            document.getElementById('world_hint').innerText = `当前世界观：【${currentActiveWorld}】，选项已自适应。`;
                        } else {
                            currentActiveWorld = "现代都市";
                            document.getElementById('world_hint').innerText = `当前为自定义世界观：【${savedWorld}】，预设词库已自动匹配为现代模版。`;
                        }
                    }
                }
            } catch(e) { console.error("读取世界观失败:", e); }

            var pSel = document.getElementById('sel_p_job');
            if(pSel) {
                pSel.innerHTML = '<option value="">- 身份 -</option>';
                DICT_JOBS[currentActiveWorld].p.forEach(job => pSel.innerHTML += `<option value="${job}">${job}</option>`);
            }
            var persSel = document.getElementById('sel_p_personality');
            if(persSel) {
                persSel.innerHTML = '<option value="">- 性格 -</option>';
                DICT_M_PERSONALITY.forEach(p => persSel.innerHTML += `<option value="${p}">${p.split(',')[0]}</option>`);
            }
            var fetishSel = document.getElementById('sel_p_fetish');
            if(fetishSel) {
                fetishSel.innerHTML = '<option value="">- 性癖 -</option>';
                DICT_M_FETISH.forEach(p => fetishSel.innerHTML += `<option value="${p}">${p}</option>`);
            }
        }

        async function loadEncounterBodies() {
            await loadWorldView();
            try {
                const sel = document.getElementById('sel_encounter_body');
                if(window.parent.Mvu) {
                    let msgId = null;
                    if (window.parent.getCurrentMessageId) msgId = window.parent.getCurrentMessageId();
                    if (msgId === null || msgId === undefined) {
                         if (window.parent.SillyTavern && window.parent.SillyTavern.getContext) {
                             const ctx = window.parent.SillyTavern.getContext();
                             if (ctx && ctx.chat && ctx.chat.length > 0) msgId = ctx.chat.length - 1;
                         }
                    }

                    if(msgId !== null) {
                        const rawVars = window.parent.Mvu.getMvuData({ type: 'message', message_id: msgId });
                        if(rawVars && rawVars.stat_data && rawVars.stat_data["夺舍躯壳"]) {
                            const bodies = rawVars.stat_data["夺舍躯壳"];
                            const bodyNames = Object.keys(bodies);

                            sel.innerHTML = '<option value="本体">本体女躯 ({{user}})</option>';
                            bodyNames.forEach(name => {
                                let opt = document.createElement('option');
                                opt.value = name;
                                opt.innerText = "分身女躯: " + name;
                                sel.appendChild(opt);
                            });
                            return;
                        }
                    }
                }
                sel.innerHTML = '<option value="本体">本体女躯 ({{user}})</option>';
            } catch(e) {
                document.getElementById('sel_encounter_body').innerHTML = '<option value="本体">本体女躯 ({{user}})</option>';
            }
        }

        loadEncounterBodies();

        function updateInput(inputId, value) { if(value) document.getElementById(inputId).value = value; }
        function getRandomItem(arr) { return arr[Math.floor(Math.random() * arr.length)]; }
        function randomSelect(selectId, inputId) {
            var sel = document.getElementById(selectId);
            var opts = Array.from(sel.options).filter(o => o.value !== "");
            var val = getRandomItem(opts).value;
            sel.value = val;
            document.getElementById(inputId).value = val;
        }

        const P_SURNAMES = ["龙", "叶", "林", "萧", "楚", "顾", "秦", "沈", "厉", "霍", "陆", "战", "皇甫", "轩辕", "王", "李"];
        const P_NAMES = ["傲天", "辰", "凡", "渊", "绝", "霆", "宇", "风", "寒", "夜", "墨", "炎", "玄", "修", "强", "伟"];

        function randomizeAll() {
            document.getElementById('p_name').value = getRandomItem(P_SURNAMES) + getRandomItem(P_NAMES);

            randomSelect('sel_p_age', 'p_age');
            randomSelect('sel_p_job', 'p_job');
            randomSelect('sel_p_face', 'p_face');
            randomSelect('sel_p_body', 'p_body');
            randomSelect('sel_p_personality', 'p_personality');
            randomSelect('sel_p_fetish', 'p_fetish');

            var len = Math.floor(Math.random() * 8) + 14;
            var wid = (Math.random() * 2 + 3.5).toFixed(1);
            document.getElementById('p_dick_size').value = "长" + len + "cm 粗" + wid + "cm";

            randomSelect('sel_p_dick_shape', 'p_dick_shape');
            randomSelect('sel_p_sperm_q', 'p_sperm_q');
            randomSelect('sel_p_sperm_v', 'p_sperm_v');
            randomSelect('sel_p_stamina', 'p_stamina');

            randomSelect('sel_scene_place', 'scene_place');
            randomSelect('sel_scene_status', 'scene_status');
        }

        function submitCreation() {
            var encouterBodyVal = document.getElementById('sel_encounter_body').value;
            var isMain = (encouterBodyVal === '本体');
            var encouterBodyDisplay = isMain ? "本体" : ("分身-" + encouterBodyVal);

            var text = "<参数设定>\n【系统提示：女主在剧情中偶遇了一位新的极品男鼎目标】\n\n";

            text += "[当前世界观]: " + currentActiveWorld + "\n\n";

            text += "[神魂寄体]\n";
            text += "本次猎艳出动的女性躯壳: " + encouterBodyDisplay + "\n\n";

            text += "[男性猎物基础参数]\n";
            text += "姓名: " + (document.getElementById('p_name').value || "未知") + "\n";
            text += "年龄: " + (document.getElementById('p_age').value || "未知") + "\n";
            text += "红尘身份: " + (document.getElementById('p_job').value || "普通男性") + "\n";
            text += "皮相相貌: " + (document.getElementById('p_face').value || "普通") + "\n";
            text += "根骨体格: " + (document.getElementById('p_body').value || "普通") + "\n";
            text += "性情本心: " + (document.getElementById('p_personality').value || "普通") + "\n";
            text += "隐秘性癖: " + (document.getElementById('p_fetish').value || "无特殊癖好") + "\n\n";

            text += "[肉体阳脉与鼎炉属性]\n";
            text += "阳气鼎炉品阶: 请AI在剧情中运用阴阳合欢功法自行观气评估（结合其肉体与精关参数）\n";
            text += "阳具尺寸: " + (document.getElementById('p_dick_size').value || "普通") + "\n";
            text += "肉茎形态: " + (document.getElementById('p_dick_shape').value || "普通") + "\n";
            text += "精液质量: " + (document.getElementById('p_sperm_q').value || "普通") + "\n";
            text += "精液数量: " + (document.getElementById('p_sperm_v').value || "普通") + "\n";
            text += "体质耐受度: " + (document.getElementById('p_stamina').value || "普通") + "\n\n";

            var extraSetting = document.getElementById('extra_setting').value.trim();
            if(extraSetting) {
                text += "[附加隐情]\n" + extraSetting + "\n\n";
            }

            var place = document.getElementById('scene_place').value || "某处";
            var manName = document.getElementById('p_name').value || "该名男子";
            var status = document.getElementById('scene_status').value || "日常状态";
            var userAction = document.getElementById('start_setting').value.trim();

            var subjectPrefix = isMain ? "{{user}}" : "{{user}}驱动女性分身" + encouterBodyVal;
            var startText = subjectPrefix + "在" + place + "游荡时，偶然发现" + manName + "正处于[" + status + "]。";

            if(userAction) {
                startText += " 随即，" + subjectPrefix + "决定：利用这具女躯的优势，" + userAction;
            } else {
                startText += " 请AI合理描写初次相遇的画面，并展示" + subjectPrefix + "运转合欢功暗中打量、评估这具男鼎阳气的过程。";
            }

            text += "[开局指令]: " + startText + "\n\n";
            text += "【请根据以上参数立刻生成一段邂逅剧情，初始化相应变量。注意区分是女主本体还是女分身在行动，并突显猎手与猎物的身份反转。】\n</参数设定>";

            if (typeof triggerSlash === 'function') {
                triggerSlash('/send ' + text + '|/trigger');
                document.querySelector('.submit-btn').innerText = "猎物已锁定...";
                document.querySelector('.submit-btn').style.background = "#2a0f12";
                document.querySelector('.submit-btn').style.color = "#8a6468";
            } else {
                alert("未检测到酒馆发送接口。");
            }
        }
    </script>
</body>
</html>
```
