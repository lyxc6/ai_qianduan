# （不要动下面的世界观）世界观控制器

**ID:** 29

**启用状态:** true

**位置:** before_char

**插入顺序:** 100

## 内容

<%_
if (typeof curWorld === 'undefined') var curWorld = getvar('stat_data.世界与本源.当前世界观', { defaults: '现代都市' });
var entryName = '世界观_' + curWorld;
_%>

<%- await getwi(null, entryName) %>
