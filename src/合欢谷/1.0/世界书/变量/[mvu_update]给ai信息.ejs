rule:
- 不准输出<变量信息>
<变量信息>
当前时间: {{format_message_variable::stat_data.当前时间}}
当前地点: {{format_message_variable::stat_data.当前地点}}
NSFW场景: {{format_message_variable::stat_data.NSFW场景}}
当前场景角色: {{format_message_variable::stat_data.当前场景角色}}
主角:
  {{format_message_variable::stat_data.主角}}
合欢谷:
  {{format_message_variable::stat_data.合欢谷}}
后宫:
  {{format_message_variable::stat_data.后宫}}
重要事件:
  {{format_message_variable::stat_data.重要事件}}
</变量信息>